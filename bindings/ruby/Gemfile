# frozen_string_literal: true

source "https://rubygems.org"

gemspec

gem "rake", "~> 13.0"
gem "rake-compiler", "~> 1.2"
# Rustクレート側の`rb-sys`とバージョンを揃える。
gem "rb_sys", "~> 0.9.128"
gem "rspec", "~> 3.13"

group :test do
  gem "actionpack", ">= 7.0"
  gem "rack-test", "~> 2.1"
  gem "railties", ">= 7.0"
  # `bundle exec rackup spec/dummy/config.ru` でdummyアプリを手動確認する用
  gem "webrick", "~> 1.9"
  # アセットパイプラインを入れた場合の経路(spec/pipeline_spec.rb)。dummyアプリ
  # 本体はパイプラインgem無しのままなので、こちらは別プロセスで読み込む。
  gem "propshaft", "~> 1.3"
end
